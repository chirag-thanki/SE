#include<stdio.h>
main()
{
	float base, height;
	printf("Enter the base:");
	scanf("%f",&base);
	printf("Enter the height:");
	scanf("%f", &height);
	printf("The area of triangle is:%f", (base*height)/2);
}
