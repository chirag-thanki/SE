#include<stdio.h>
main()
{
	int length, breadth;
	printf("Enter the length:");
	scanf("%d",&length);
	printf("Enter the breadth:");
	scanf("%d", &breadth);
	printf("The area of rectangle is:%d", length*breadth);
}
